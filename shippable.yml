language: go

#runtime:
 # nodePool: default_node_pool
  
go:
  - 1.10.3

services:
  - mysql
  #- selenium
  #- postgres
  #- redis
  #- rabbitmq
  #- mongodb
  #- memcached
  #- riak
  #- elasticsearch
  #- couchdb
  #- cassandra
  #- neo4j



build:

 pre_ci_boot:
   image_name: drydock/u16golall
   image_tag: v6.7.4
   pull: true


 ci:
   #- mysql -e "CREATE USER redacted@localhost IDENTIFIED BY 'redacted';"
   - mysql -e "CREATE USER classifier@localhost IDENTIFIED BY 'foobar';"
   #- java --version
   - mysql --version
   #- sqlite3 --version
   #- psql --version
   #- redis-server --version
   #- dpkg -s rabbitmq-server | grep Version
   #- mongod --version
   #- memcached --version
   #- riak version
   #- elasticsearch --version
   #- sleep 5
   #- curl http://127.0.0.1:5984
   #- cassandra -v
   #- pip show pycrypto
   #- java -jar /usr/local/selenium/selenium-server-standalone-3.141.59.jar --version

